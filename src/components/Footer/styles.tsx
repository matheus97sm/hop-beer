import styled from 'styled-components';

export const FooterWrapper = styled.footer`
  width: 100%;
  padding: 16px 0;

  p {
    width: 100%;

    color: var(--orange);
    text-align: center;

    a {
      color: var(--orange);
      font-weight: 900;
    }
  }
`;
